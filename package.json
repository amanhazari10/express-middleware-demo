{
  "name": "express-middleware-demo",
  "version": "1.0.0",
  "description": "Express.js server with logging middleware and bearer token authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "middleware",
    "logging",
    "authentication",
    "bearer-token"
  ],
  "author": "amanhazari10",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
